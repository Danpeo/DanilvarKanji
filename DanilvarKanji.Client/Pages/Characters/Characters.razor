@page "/Characters"

@if (CharacterItems == null)
{
    <DisplayLoading></DisplayLoading>
}
else
{
    <h3 class="mb-5 display-3 txt-white">Кандзи</h3>
    
    if (CharacterItems.Any())
    {
        <div class="row mt-3">
            <DisplayCharacters CharacterItems="CharacterItems"/>
        </div>
    }
}